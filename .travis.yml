sudo: true
dist: trusty

jobs:
  include:
    - stage: build docker image
      script:
 
      - docker build -t faileder/dgo-frontend:$TRAVIS_BRANCH .
      - docker login -u failender -p $DOCKER_PASS
      - docker push faileder/dgo-frontend

